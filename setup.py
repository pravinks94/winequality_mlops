from setuptools import setup,find_packages

setup(
    name = "src",
    version = "0.0.1", 
    description = "wine quality prediction with the help of MLOps",
    author = "pravinks94",
    packages = find_packages(),
    license = "MIT",
    )

